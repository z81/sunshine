import { render } from 'react-dom';
import React from 'react';
import { Button } from '@sunshine/sunshine-ui';

render(
  <div>
    <Button>btn</Button>
  </div>,
  document.getElementById('root'),
);
